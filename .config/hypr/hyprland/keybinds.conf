# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  KEYBINDS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

$mainMod = SUPER
$terminal = kitty
$menu = hyprlauncher
$browser = firefox
$filemanager = $terminal -e yazi
$screenshot = hyprshot -m output -o ~/Pictures/Screenshots -f Screenshot_Full_$(date +%Y-%m-%d_%H-%M-%S).png

bind = $mainMod, Q, exec, $terminal
bind = $mainMod, R, exec, $menu
bind = $mainMod, B, exec, $browser
bind = $mainMod, E, exec, $filemanager

bind = , Print, exec, $screenshot

bind = $mainMod, C, killactive,
bind = $mainMod, Escape, exec, hyprctl dispatch dpms toggle
bind = $mainMod, M, exec, sleep 1 && hyprlock

bind = $mainMod, F, fullscreen, 0
bind = $mainMod, M, fullscreen, 1
bind = $mainMod, V, togglefloating,
bind = $mainMod, D, pseudo,

bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bind = $mainMod, left, layoutmsg, focus l
bind = $mainMod, right, layoutmsg, focus r
bind = $mainMod, up, layoutmsg, focus u
bind = $mainMod, down, layoutmsg, focus d

bind = $mainMod SHIFT, left, layoutmsg, swapcol l
bind = $mainMod SHIFT, period, layoutmsg, swapcol r
bind = $mainMod SHIFT, up, layoutmsg, swapnext
bind = $mainMod SHIFT, down, layoutmsg, swapprev

bind = $mainMod ALT, K, layoutmsg, consume
bind = $mainMod ALT, L, layoutmsg, expel

# ───────── Switch  workspace ─────────
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# ───────── Special workspace ─────────
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# ───────── Window Mooving ─────────
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10

# ───────── Window Management ─────────
#binde = $mainMod&SHIFT_L, left, resizeactive,-50 0
#binde = $mainMod&SHIFT_L, right, resizeactive,50 0
#binde = $mainMod&SHIFT_L, up, resizeactive,0 -50
#binde = $mainMod&SHIFT_L, down, resizeactive,0 50